version: "3"
services:
  client:
    image: "partical1/partical_client:latest"
    restart: always
    ports:
      - "8080:8080"
    links:
      - redis
  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
